# Core runtime dependencies — fully pinned for reproducible builds.
# These are everything needed for mock mode (no GPU, no model downloads).
chainlit==2.11.1
openai==2.37.0
chromadb==1.0.21
psycopg2-binary==2.9.12
asyncpg==0.31.0
SQLAlchemy==2.0.49
bcrypt==5.0.0
python-dotenv==1.0.1
