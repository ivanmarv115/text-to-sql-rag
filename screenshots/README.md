# Screenshots

Add screenshots/GIFs of the running demo here and reference them from the
top-level `README.md`.

Suggested captures (run `docker compose up`, open http://localhost:8000, log in
with `demo` / `demo`):

- `chat-mock.png` — answering *"How many patients are there?"* in mock mode
- `multilingual.png` — answering *"¿Cuántos pacientes hay?"*
- `debug-trace.png` — the Text-to-SQL trace step with `DEBUG_CHAT=true`
  (shows retrieved tables and the generated SQL)
- `safety-block.png` — a blocked non-read-only query

> _Placeholder — no images are committed yet._
